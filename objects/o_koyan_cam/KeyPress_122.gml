/// @desc (Full_Screen) Button
// You can "CHANGE" the button whatever you like, this is just default (F11)
// This is for later if you want to adjust the setting's of fullscreen inside the game so i've put a button for it
global._fullscreen = global._fullscreen == true ? false : true;

//  |---------------|
// <| Made by Koyan |>
//  |---------------|